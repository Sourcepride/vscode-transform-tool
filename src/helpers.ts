export function getCurrentTheme() {
  // const themeKind = vscode.window.activeColorTheme;
  // // Determine if the theme is light or dark based on its name
  // if(themeKind.kind === vscode.ColorThemeKind.Dark){
  //   vscode.setS({ count });
  // }
}
